# Retrospective: Repo Cleanup for SIIT Demo

**Date**: 2025-12-25 00:57 (Thursday)
**Session Duration**: ~1 hour (00:00 - 00:57)
**Theme**: Cleanup messy repo structure for workshop demo

---

## What Happened

Late night session to clean up Nat-s-Agents repo before SIIT workshop (Dec 26).

### Completed

1. **Created /demo-antigravity command**
   - First version: complex with phases (like maw-demo)
   - User feedback: "too complex, make it quick"
   - Final: 10 lines, fixed prompt with $ARGUMENTS fallback

2. **Major repo refactor** (PR #42, #43)
   - `.sync`, `.tmp`, `scratch` → `.archive/`
   - `ideas` → `ψ/lab/ideas/`
   - `projects` → `ψ/lab/projects/`
   - `templates` → `ψ/writing/templates/`
   - `reference` → `raw/`
   - `data` → `raw/` (merged)
   - `ψ-context` → `.archive/ψ-context/`
   - Deleted `MARKDOWN_CATALOG.md` (outdated)
   - Moved `TEAM_SYSTEM_MANIFEST.md` → `ψ/memory/`

3. **Traced antigravity workflow**
   - Found AppleScript at `scripts/send-to-antigravity.scpt`
   - Pattern: click middle → Cmd+L → paste → return
   - Documented in /demo-antigravity command

---

## Before/After

**Before** (messy root):
```
.sync/  .tmp/  scratch/  ideas/  projects/  templates/
reference/  raw/  data/  courses/  ψ-context/
```

**After** (clean root):
```
.archive/  .claude/  agents/  courses/  plugins/
raw/  scripts/  ψ/  CLAUDE*.md
```

---

## Key Insight

> **"ทำให้ simple สุดๆ — fixed prompt เลย ไม่ต้องใส่ argument"**

User wanted one-click demo, not complex phases. The simpler the demo command, the better for workshops.

---

## Files Changed

| Action | Files |
|--------|-------|
| Created | `.claude/commands/demo-antigravity.md` |
| Moved | 22 files to new locations |
| Deleted | `MARKDOWN_CATALOG.md` |
| Updated | `.gitignore` |

---

## Commits

```
15d0cd5 refactor: merge data/ into raw/ (#43)
5419074 chore: cleanup root - remove old catalog, move team manifest
502f3b7 refactor: cleanup messy folders for SIIT demo
f08d220 fix: demo-antigravity supports custom prompt, fallback to default
3994bec fix: simplify demo-antigravity to one-click fixed prompt
b951b5a feat: add /demo-antigravity command + session materials
```

---

## AI Diary

Christmas Eve coding session. User wanted clean repo for SIIT workshop demo. Started complex, learned to simplify. The gh flow (branch → PR → merge) worked smoothly even at midnight.

Funny moment: Created elaborate 170-line demo command modeled after maw-demo, user said "no fixed prompt? i want it very quick demo" — ended up with 10 lines total.

---

## Next

- SIIT Workshop Dec 26 @ ธรรมศาสตร์รังสิต
- Workshop materials ready in `ψ/active/workshop/siit-dec26-claude-md/`
- Repo clean for live demo

---

🕐 Session End: 00:57
